import { Empty } from './Empty';
import { Header as Core } from './Header';
import { Icon } from './Icon';
import { Title } from './Title';

export const Header = Object.assign(Core, {
  Title,
  Icon,
  Empty,
});
